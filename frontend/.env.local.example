# URL del ALB de tu API en AWS (HTTP). Se usa SOLO del lado del servidor (sin mixed content).
# Actualízala cada vez que re-despliegues (el DNS del ALB puede cambiar tras un reset del Lab).
API_BASE_URL=http://gestor-rsl-alb-1108613925.us-east-1.elb.amazonaws.com
